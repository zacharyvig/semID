# semID 0.4.0

* Implemented the `id2()` function which evaluates a model's identification status
  using the two-step rule of identification.
* Expanded and reorganized the test suite to cover rule outputs, printing methods,
	scaling output, two-step ID output, and model classification.

